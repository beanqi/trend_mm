# AGENTS.md

目标：正确实现当前需求，并持续降低代码复杂度。

## 1. 开发前

- 阅读 `docs/` 中与需求相关的文档和现有实现。
- 理清数据流、状态归属和模块边界后再修改。
- 现有结构能解决问题时，不改架构；不为未来需求预留能力。

## 2. 设计原则

- 简单、具体、显式；小改优先于重写，删除优先于新增。
- 只增加当前需求需要的代码、类型、模块、状态、依赖和配置。
- 仅在抽象能明显降低整体复杂度时引入抽象；Trait 只用于真实存在的多实现。
- 优先普通 `struct`、`enum`、函数和显式 `match`。
- 不隐藏重要控制流、网络调用、状态变化和资金操作。

## 3. 修改与验证

- 限制文件范围、接口变化、新概念和新增代码；不重构无关代码。
- 函数职责单一，名称表达业务含义，错误包含必要上下文。
